# Minimal HOCON used by CI to exercise CheckPolicies against a stable, keyless setup (Ollama).
llm4s {
  providers {
    provider = "ollama-smoke"
    ollama-smoke {
      provider = "ollama"
      model = "llama3"
      baseUrl = "http://localhost:11434"
    }
  }
}
